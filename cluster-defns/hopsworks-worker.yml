  worker__WORKER_ID__:
  
    size: 1
    baremetal:
      ip: __WORKER_IP__
    attrs:
      ndb:
        __NDB_NVME__  
        NoOfReplicas: 1
        LockPagesInMainMemory: 0
        TotalMemoryConfig: 16G
        NumCPUs: 8
    #    diskdata_dir: "/mnt/nvmeDisks/nvme0/"
    #    nvme:
    #      logfile_size: "10G"
      cuda:
        accept_nvidia_download_terms: false
      hops:
        yarn:
          pcores-vcores-multiplier: 1.0
          system-reserved-memory-mb: 750
    recipes:
      - kagent
      - conda
      - ndb::ndbd
      # - hops::dn
      # - hops::nm
      - hadoop_spark::yarn
      # - flink::yarn
      # - hopslog::_filebeat-spark
      - hopslog::_filebeat-services
      # - tensorflow
      - hopsmonitor::node_exporter
      # - livy::install
      - consul::slave
#      - kube-hops::node


